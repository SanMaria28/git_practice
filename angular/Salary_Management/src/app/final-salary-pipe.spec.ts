import { FinalSalaryPipe } from './final-salary-pipe';

describe('FinalSalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new FinalSalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
