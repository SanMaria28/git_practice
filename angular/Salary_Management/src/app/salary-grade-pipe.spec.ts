import { SalaryGradePipe } from './salary-grade-pipe';

describe('SalaryGradePipe', () => {
  it('create an instance', () => {
    const pipe = new SalaryGradePipe();
    expect(pipe).toBeTruthy();
  });
});
